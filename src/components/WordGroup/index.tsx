import { useState } from 'react';

const WordGroup = () => {
  const [words, setWords] = useState<string[][]>([]);

  return (
    <div>
      <div></div>
    </div>
  );
};

export default WordGroup;
